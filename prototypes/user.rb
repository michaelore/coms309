class User

	# This is the user class -- in the future routes should be an array of routes.
  def initialize(email,pwhash,id,routes[])
    @email = email
    @pwhash = pwhash
    @id = id
	routes[] = routes[]
  end
end
